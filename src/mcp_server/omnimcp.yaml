# omnimcp.ai configuration
startCommand:
  type: stdio
configSchema:
  type: object
  properties: {}
commandFunction:
  # A function that produces the CLI command to start the MCP on stdio.
  |-
  (config) => ({
    command: "node",
    args: [
      "build/index.js"
    ],
    env: {
      // Define any additional environment variables if necessary
    }
  })