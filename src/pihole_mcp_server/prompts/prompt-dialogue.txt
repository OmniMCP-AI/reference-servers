They will interact via another chat bot calling its api
We will use NER to look for special names, ip addresses, domain names etc but otherwise straight forward RAG using pymilvus as the vector store. We should generally be language agnostic (English, Japanese, French or Chinese) on the input to assess the intent, and use simple english "api references" to capture the intent of each api endpoint/parameters, so that the vectordb can match on nearest neighbor
An example for the `POST /gravity` API call could be: "Run a gravity update to refresh the block lists and remove any expired entries. This call does not require any parameters." This concise description captures the essence of the action and can be embedded in the vector database for intent matching.
The MCP server should be configured with its own Pi-hole credentials, such as an application password, to act on behalf of users. This approach simplifies the user interaction and enhances security by preventing the need to expose user credentials for every request. Additionally, implementing a secure token mechanism for session management could further protect user data.
Lets use a simple API key as the number of clients will be small
The chatbot client should present the API key in the HTTP header, preferably using `X-API-Key: <your_key>`. This method keeps the key out of the URL, providing a little more security by preventing it from being logged in HTTP referrer headers.
The intelligence should be on the server side, lets go with option A
Option 2 seems the most useful, as it provides both a user-friendly message and the raw data/status from the Pi-hole API. This way, the chatbot can give the user a concise summary while also having access to more detailed information if needed.
We should focus on dns blocking, group management and exporting statistics.
Thats a good point - lets include the optional modify/get-specific/batch-delete endpoints as well
For the enable/disabled prompt, the intent prompt should be Please <enable|disable> pihole [for N ]; where N can be minutes, seconds or hours
The simple English API reference for the `GET /blocking` endpoint should be: "Check if Pi-hole blocking is currently enabled or disabled. This endpoint retrieves the blocking status and indicates if there is an active timer. Use this to confirm the current state of the Pi-hole`s blocking feature."
For the enable/disabled prompt, the intent prompt should be Please <enable|disable> pihole [for N ]; where N can be minutes, seconds or hours
The simple English API reference for the `POST /blocking` endpoint should be: "Use this endpoint to enable or disable Pi-hole blocking. You can also optionally set a timer to temporarily disable blocking for a specified duration (in seconds). This allows for flexible management of blocking functionality."
The simple English API reference for the `GET /api/groups` endpoint should be: "Retrieve a list of all configured groups in Pi-hole. This endpoint provides an overview of the current group settings, allowing users to see all available groups at a glance."
The simple English API reference for the `GET /api/groups/{name}` endpoint should be: "Retrieve detailed information about a specific group in Pi-hole using its name. This endpoint provides the groups ID, name, comment, enabled status, and relevant dates. The group name must be included as a parameter in the URL."
The simple English API reference for the `POST /api/groups/direct` endpoint should be: "Use this endpoint to create one or more new groups in Pi-hole. The request body can contain a single `name` or an array of `name`s, along with optional `comment` and `enabled` status. This allows users to efficiently manage group creation in a single request."
The simple English API reference for the `PUT /api/groups/{name}` endpoint should be: "Use this endpoint to modify an existing group in Pi-hole identified by its name. You can change the groups name (effectively renaming it), `comment`, or its `enabled` status by providing the desired properties in the request body. This allows for easy updates to group attributes."
The simple English API reference for the `PUT /api/groups/{name}` endpoint should be: "Use this endpoint to modify an existing group in Pi-hole identified by its name. You can change the groups name (effectively renaming it), `comment`, or its `enabled` status by providing the desired properties in the request body. This allows for easy updates to group attributes."
The simple English API reference for the `PUT /api/groups/{name}` endpoint should be: "Use this endpoint to modify an existing group in Pi-hole identified by its name. You can change the groups name (effectively renaming it), `comment`, or its `enabled` status by providing the desired properties in the request body. This allows for easy updates to group attributes."
The simple English API reference for the `PUT /api/groups/{name}` endpoint should be: "Use this endpoint to modify an existing group in Pi-hole identified by its name. You can change the groups name (effectively renaming it), `comment`, or its `enabled` status by providing the desired properties in the request body. This allows for easy updates to group attributes."
The simple English API reference for the `DELETE /api/groups/{name}` endpoint should be: "Use this endpoint to delete an existing group in Pi-hole identified by its name. You can change the groups name (effectively renaming it), `comment`, or its `enabled` status by providing the desired properties in the request body. This allows for easy deletes to group attributes."
The simple English API reference for the `DELETE /api/groups/{name}` endpoint should be: "Use this endpoint to delete an existing group in Pi-hole identified by its name. This allows for easy deletes of a group."
The simple English API reference for the `POST /api/groups/batchDelete` endpoint should be: "Use this endpoint to delete multiple existing groups in Pi-hole by providing an array of group names in the request body. This allows for efficient removal of several groups at once."
The simple English API reference for the `GET /summary` endpoint should be: "Use this endpoint to retrieve a 24-hour overview of statistics in Pi-hole, including total queries, blocked queries, percentage blocked, unique domains, forwarded queries, cached queries, query frequency, counts per query type/status/reply, active clients, total clients, gravity domain count, and last gravity update time."
The simple English API reference for the `GET /top_domains` endpoint should be: "Use this endpoint to retrieve the most frequently queried domains in Pi-hole. You can specify the `blocked=true` query parameter to get the most frequently blocked domains instead. Additionally, you can control how many domains are returned by using the `count` query parameter."
The simple English API reference for the `GET /top_domains` endpoint should be: "Use this endpoint to retrieve the most frequently queried domains in Pi-hole. You can specify the `blocked=true` query parameter to get the most frequently blocked domains instead. Additionally, you can control how many domains are returned by using the `count` query parameter."
The simple English API reference for the `GET /top_domains` endpoint should be: "Use this endpoint to retrieve the most frequently queried domains in Pi-hole. You can specify the `blocked=true` query parameter to get the most frequently blocked domains instead. Additionally, you can control how many domains are returned by using the `count` query parameter."
The simple English API reference for the `GET /top_clients` endpoint should be: "Use this endpoint to retrieve the clients that have made the most queries in Pi-hole. You can specify the `blocked=true` query parameter to get the clients whose queries were blocked most often. Additionally, you can control how many clients are returned by using the `count` query parameter."
The simple English API reference for the `GET /recent_blocked` endpoint should be: "Use this endpoint to retrieve the domain name(s) that were most recently blocked by Pi-hole. You can control how many recent domains are returned by using the `count` query parameter, which defaults to 1 if omitted."
For the endpoints we've scoped (DNS blocking, Group Management, Statistics), the parameters that need to be extracted from the user's natural language query using Named Entity Recognition (NER) include: duration (e.g., "10 minutes"), enable/disable flag, group name, target group name, new group name (optional), comment (optional), enabled status (optional), and count (optional). Please let me know if there are any additional parameters or details to consider.
For the endpoints we have scoped (DNS blocking, Group Management, Statistics), the parameters that need to be extracted from the users natural language query using Named Entity Recognition (NER) include: duration (e.g., "10 minutes"), enable/disable flag, group name, target group name, new group name (optional), comment (optional), enabled status (optional), and count (optional). Please let me know if there are any additional parameters or details to consider.
yes it does, we should have natural language names for each of the parameters
yes, they look good
 how about 403? Unauthoried
lets go with 404
You make a good point, lets go with 502 Bad Gateway instead.
For scenario 3, let's go with 400 Bad Request to indicate the user's query was unclear.
For scenario 3, lets go with 400 Bad Request to indicate the users query was unclear.
For scenario 4, lets go with 422 Unprocessable Entity to indicate a required parameter is missing.
For scenario 5, lets go with 500 Internal Server Error to indicate a general server-side issue when calling the Pi-hole API.
For `GET /blocking`, here are 4 distinct ways a user might ask to check the current blocking status: 1. "Is Pi-hole blocking ads?", 2. "Whats the blocking status?", 3. "Can you tell me if Pi-hole is currently blocking?", 4. "Check if ads are being blocked by Pi-hole."
For `GET /api/groups`, here are 4 distinct ways a user might ask to list all the configured groups: 1. "Can you show me all the groups?", 2. "What groups are configured?", 3. "List all groups in Pi-hole.", 4. "Show me the current group settings."
For `GET /api/groups/{name}`, here are 4 distinct ways a user might ask for the details of a specific group: 1. "What are the details for group `<group_name>`?", 2. "Can you provide information about `<group_name>`?", 3. "Show me the settings for `<group_name>` group.", 4. "Tell me about the group named `<group_name>`."
For `POST /api/groups/direct`, here are 4 distinct ways a user might ask to create a new group: 1. "Create a group named `<group_name>` with comment `<comment>` and set it to `<status: enabled/disabled>`.", 2. "Add a new group called `<group_name>`, enabled by default.", 3. "Could you create multiple groups: `<group_name1>`, `<group_name2>` with comments `<comment1>`, `<comment2>`?", 4. "I want to set up a group named `<group_name>`, please."
For `PUT /api/groups/{name}`, here are 4 distinct ways a user might ask to change the properties of an existing group: 1. "Rename `<current_group_name>` to `<new_group_name>` and set the comment to `<comment>`.", 2. "Update the `<current_group_name>` group to be `<status: enabled/disabled>`.", 3. "Can you change the properties of `<current_group_name>` to have a new name `<new_group_name>` and comment `<comment>`?", 4. "Modify group `<current_group_name>` to enable it and add comment `<comment>`."
For `PUT /api/groups/{name}`, here are 4 distinct ways a user might ask to change the properties of an existing group: 1. "Rename `<current_group_name>` to `<new_group_name>` and set the comment to `<comment>`.", 2. "Update the `<current_group_name>` group to be `<status: enabled/disabled>`.", 3. "Can you change the properties of `<current_group_name>` to have a new name `<new_group_name>` and comment `<comment>`?", 4. "Modify group `<current_group_name>` to enable it and add comment `<comment>`."
For `DELETE /api/groups/{name}`, here are 4 distinct ways a user might ask to delete a specific group: 1. "Can you delete the group named `<group_name>`?", 2. "I want to remove the `<group_name>` group.", 3. "Please delete the group called `<group_name>`.", 4. "Remove `<group_name>` from the list of groups."
For `POST /api/groups/batchDelete`, here are 4 distinct ways a user might ask to delete several groups at once: 1. "Can you delete the groups `<group_name1>`, `<group_name2>` and `<group_name3>`?", 2. "I want to remove the groups `<group_name1>` and `<group_name2>`.", 3. "Please delete the groups named `<group_name1>`, `<group_name2>`, and `<group_name3>`.", 4. "Remove `<group_name1>`, `<group_name2>`, and `<group_name3>` from my account."
For `GET /summary`, here are 4 distinct ways a user might ask for the overall summary of Pi-hole activity: 1. "Can you provide the 24-hour summary statistics for Pi-hole?", 2. "I want to see the summary of Pi-hole activity over the last day.", 3. "Please give me the overall summary statistics for the past 24 hours regarding Pi-hole.", 4. "Show me the summary of activity in the last day for Pi-hole."
For `GET /top_domains`, here are 4 distinct ways a user might ask for the most frequent domains, potentially specifying blocked/permitted or a count: 1. "Can you provide the top `<count>` blocked domains?", 2. "I want to see the most permitted domains, limit it to `<count>`.", 3. "Show me the top `<count>` domains that were blocked.", 4. "What are the most frequent domains, both blocked and permitted, for the last `<count>` requests?"
For `GET /top_clients`, here are 4 distinct ways a user might ask for the most active clients, potentially specifying blocked/active or a count: 1. "Can you provide the top `<count>` active clients?", 2. "I want to see the most blocked clients, limit it to `<count>`.", 3. "Show me the top `<count>` clients that are currently active.", 4. "What are the most frequent clients, both blocked and active, for the last `<count>` requests?"
For `GET /recent_blocked`, here are 4 distinct ways a user might ask for the last domain(s) that were blocked: 1. "Can you show me the most recent `<count>` blocked domains?", 2. "I want to see the last `<count>` domains that were blocked.", 3. "Please give me the most recently blocked domains, limit it to `<count>`.", 4. "What are the latest `<count>` domains added to the blocklist?"
We should have a configuration file allowing endpoints to be disabled/enabled, and logging level to be specified as well as logging to go both to stdout/stderr and a file
 lets go with toml
 yes it does. 501 works for error codes
